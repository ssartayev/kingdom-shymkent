# Kingdom Shymkent — лендинг

Один автономный файл: [dist/index.html](dist/index.html)
Стек: чистый HTML + CSS + vanilla JS, GSAP с CDN (анимации появления секций, счётчики статистики, плавающие "орбы" фона).

## Запуск

```bash
cd dist && python3 -m http.server 8000
```
Открыть http://localhost:8000

## Проверка перед сборкой

```bash
node verify.js
```
Скрипт [verify.js](verify.js) проверяет:
- парность HTML-тегов
- ровно 20 `<line>` элементов (2 логотипа × 10 линий короны)
- ровно 2 группы `class="wordmark"`
- ровно 2 группы `class="crown-lines"`
- равное количество `data-kaz` и `data-ru` атрибутов

## Двуязычность (ҚАЗ / РУС)

- По умолчанию язык — казахский (`localStorage` пуст → `kaz`).
- Переключатель в навигации: кнопки `.lang-btn` с `data-lang="kaz"` / `data-lang="ru"`.
- Любой текстовый элемент, который должен переводиться, получает класс `t` и атрибуты `data-kaz="..."` и `data-ru="..."`.
- Функция `setLanguage(lang)` (в `<script>` внизу файла):
  - проходит по всем `.t`,
  - для `<input>`/`<textarea>` подставляет значение в `placeholder`,
  - для остальных элементов — в `textContent`,
  - подсвечивает активную кнопку переключателя,
  - сохраняет выбор в `localStorage.kingdom_lang`.
- При загрузке страницы читается `localStorage.kingdom_lang`, иначе — `kaz`.

**Важно:** при добавлении нового текста всегда добавляй оба атрибута (`data-kaz` и `data-ru`) на один и тот же элемент — иначе `verify.js` сообщит о несовпадении количества.

## Плейсхолдеры, которые нужно заменить перед запуском

| Что | Где искать | На что заменить |
|---|---|---|
| ~~WhatsApp-номер~~ | `var WA_NUMBER = '77053396313'` в начале `<script>` — **уже реальный**, менять только при смене номера | — |
| ~~Телефон~~ | `+7 (705) 339-63-13` в контактах — **уже реальный** | — |
| `© 2026 Kingdom Shymkent` | footer | при необходимости — год/название |
| Текст "Мәтін орны" / "Место для текста" | курсы, "О нас", учителя, отзывы, контакты (адрес) | реальные описания, тексты отзывов, био учителей |
| "Аты-жөні" / "Имя Фамилия" | учителя (3), отзывы (5) | реальные имена |
| "Жетістік орны" / "Место достижения" (3 шт. в hero) | блок `.stats` | реальные подписи к цифрам (например "учеников", "лет на рынке", "сдали экзамен") — сами цифры (`data-count="500"`, `12`, `98`) тоже заменить на актуальные |
| 2 карты "2ГИС картасының орны (1/2-филиал)" | секция "Байланыс/Контакты", блок `.maps-grid` — две карточки `.branch`, в каждой `.map-frame` с HTML-комментарием-подсказкой | виджеты 2ГИС: на 2gis.kz найти филиал → «Поделиться» → «Виджет на сайт», вставить `<iframe>` вместо `<span>` |
| "Мәтін орны (мекенжай)" × 2 | заголовки филиалов в `.branch-head` | реальные адреса филиалов |
| Логотип/название | по умолчанию используется встроенный SVG-кроун + надпись KINGDOM (см. фото) | при необходимости заменить на реальный файл логотипа |

## Структура секций (по образцу лидеров рынка: InterPress, JSI)

`#top` (hero + бегущая строка) → блок цифр → `#courses` (5 курсов, клик по курсу = заявка в WhatsApp) → `#about` (тёмный блок, преимущества) → `#teachers` (3) → `#reviews` (5, горизонтальный свайп) → `#faq` (аккордеон, 5 вопросов) → `#form` (заявка → WhatsApp) → `#contacts` → footer.

## Как работают заявки

Бэкенда нет — всё через WhatsApp (реально работает сразу):
- Все элементы с классом `.wa-link` (hero-кнопка, контакты, плавающая кнопка) открывают `wa.me/<WA_NUMBER>` с приветственным сообщением на текущем языке.
- Клик по строке курса открывает WhatsApp с сообщением «хочу на пробный урок курса X».
- Форма `#leadForm` валидирует телефон (маска +7, 11 цифр) и открывает WhatsApp с готовой заявкой: имя, телефон, курс, время. После отправки показывается «Спасибо».

## Логотип

SVG `viewBox="0 0 160 158"`, три копии в файле (nav, hero-карточка, footer):
- `.crown-lines` — 10 `<line>` (корона), stroke `#EAF0FF`, stroke-width `2.6`, linecap round.
- `.wordmark` — надпись KINGDOM штриховыми `<path>`/`<rect>`, stroke `#EAF0FF`, stroke-width `2`.

Координаты — не менять без необходимости (проверяются `verify.js`).
